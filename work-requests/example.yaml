work_request:
  id: "WR-2026-001"
  created: "2026-01-10T17:00:00Z"

  from_team: "structure_engineering"
  from_agent: "SE-Coordinator"
  to_team: "platform_engineering"

  type: "api_addition"
  priority: "high"
  blocking: true

  title: "Add Department CRUD API Endpoints"
  description: |
    Need new API endpoints for department management:
    - GET /api/departments - List all departments
    - POST /api/departments - Create department
    - PUT /api/departments/:id - Update department
    - DELETE /api/departments/:id - Delete department

  acceptance_criteria:
    - "Endpoints return JSON matching DepartmentV2 schema"
    - "POST validates required fields"
    - "DELETE handles cascading team reassignment"
    - "All endpoints have TypeScript types"

  status: "pending"
